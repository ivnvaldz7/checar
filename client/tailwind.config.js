// Tailwind v4 — configuración mínima.
// El sistema de colores y tipografía se define en src/index.css vía @theme,
// usando los mismos valores que src/tokens.js (fuente de verdad para JS).

/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
}
